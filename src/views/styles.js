import { css } from 'emotion';

const contentWrapper = css`
  display: flex;
  height: 100%;
`;

const mapWrapper = css`
  padding-left: 15px;
  padding-right: 15px;
`;

const listingWrapper = css`
  padding-right: 15px;
`;

export default {
  contentWrapper,
  listingWrapper,
  mapWrapper
}